{{#each paths}}
    {{#each responses}}
        {{#with_matching @key 'int32' 'int' 'int64' 'long'}}

public class {{../../this.operationId}}Exception : Exception, IHasHttpServiceError
{
    public {{../../this.operationId}}Exception()
        : base() { }

    public {{../../this.operationId}}Exception(string message)
        : base(message) { }

    public {{../../this.operationId}}Exception(string message, Exception innerException)
        : base(message, innerException) { }

    public HttpServiceError HttpServiceError { get { return HttpServiceErrorDefinition.GeneralError; } }
}
        {{/with_matching}}
    {{/each}}
{{/each}}
